# This file is part of the Spectrum +4 Project.
# Licencing information can be found in the LICENCE file
# (C) 2021 Spectrum +4 Authors. All rights reserved.

# This file is auto-generated by test_po_tokens.sh. DO NOT EDIT!

.text




# Test po_tokens_0f00 tests PO_TOKENS when passed A=0x00 (BASIC keyword "RND")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "RND".
po_tokens_0f00_setup:
  jp      fake0

po_tokens_0f00_setup_regs:
  ld      a, 0x00
  ld      iy, 0x5000
  ret

po_tokens_0f00_effects:
  ld      de, msg_po_tokens_0f00
  call    print_msg_de
  ret

po_tokens_0f00_effects_regs:
  push    af
  pop     de
  ldf     C_FLAG|N_FLAG|H_FLAG|S_FLAG
  ret

msg_po_tokens_0f00: .asciz "RND"


# Test po_tokens_0f01 tests PO_TOKENS when passed A=0x01 (BASIC keyword "INKEY$")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "INKEY$".
po_tokens_0f01_setup:
  jp      fake0

po_tokens_0f01_setup_regs:
  ld      a, 0x01
  ld      iy, 0x5000
  ret

po_tokens_0f01_effects:
  ld      de, msg_po_tokens_0f01
  call    print_msg_de
  ret

po_tokens_0f01_effects_regs:
  push    af
  pop     de
  ldf     C_FLAG|N_FLAG|H_FLAG|S_FLAG
  ret

msg_po_tokens_0f01: .asciz "INKEY$"


# Test po_tokens_0f02 tests PO_TOKENS when passed A=0x02 (BASIC keyword "PI")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "PI".
po_tokens_0f02_setup:
  jp      fake0

po_tokens_0f02_setup_regs:
  ld      a, 0x02
  ld      iy, 0x5000
  ret

po_tokens_0f02_effects:
  ld      de, msg_po_tokens_0f02
  call    print_msg_de
  ret

po_tokens_0f02_effects_regs:
  push    af
  pop     de
  ldf     C_FLAG|N_FLAG|H_FLAG|S_FLAG
  ret

msg_po_tokens_0f02: .asciz "PI"


# Test po_tokens_0f03 tests PO_TOKENS when passed A=0x03 (BASIC keyword "FN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "FN ".
po_tokens_0f03_setup:
  jp      fake0

po_tokens_0f03_setup_regs:
  ld      a, 0x03
  ld      iy, 0x5000
  ret

po_tokens_0f03_effects:
  ld      de, msg_po_tokens_0f03
  call    print_msg_de
  ret

po_tokens_0f03_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|Z_FLAG
  ret

msg_po_tokens_0f03: .asciz "FN "


# Test po_tokens_0f04 tests PO_TOKENS when passed A=0x04 (BASIC keyword "POINT")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "POINT ".
po_tokens_0f04_setup:
  jp      fake0

po_tokens_0f04_setup_regs:
  ld      a, 0x04
  ld      iy, 0x5000
  ret

po_tokens_0f04_effects:
  ld      de, msg_po_tokens_0f04
  call    print_msg_de
  ret

po_tokens_0f04_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f04: .asciz "POINT "


# Test po_tokens_0f05 tests PO_TOKENS when passed A=0x05 (BASIC keyword "SCREEN$")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "SCREEN$ ".
po_tokens_0f05_setup:
  jp      fake0

po_tokens_0f05_setup_regs:
  ld      a, 0x05
  ld      iy, 0x5000
  ret

po_tokens_0f05_effects:
  ld      de, msg_po_tokens_0f05
  call    print_msg_de
  ret

po_tokens_0f05_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f05: .asciz "SCREEN$ "


# Test po_tokens_0f06 tests PO_TOKENS when passed A=0x06 (BASIC keyword "ATTR")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ATTR ".
po_tokens_0f06_setup:
  jp      fake0

po_tokens_0f06_setup_regs:
  ld      a, 0x06
  ld      iy, 0x5000
  ret

po_tokens_0f06_effects:
  ld      de, msg_po_tokens_0f06
  call    print_msg_de
  ret

po_tokens_0f06_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f06: .asciz "ATTR "


# Test po_tokens_0f07 tests PO_TOKENS when passed A=0x07 (BASIC keyword "AT")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "AT ".
po_tokens_0f07_setup:
  jp      fake0

po_tokens_0f07_setup_regs:
  ld      a, 0x07
  ld      iy, 0x5000
  ret

po_tokens_0f07_effects:
  ld      de, msg_po_tokens_0f07
  call    print_msg_de
  ret

po_tokens_0f07_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f07: .asciz "AT "


# Test po_tokens_0f08 tests PO_TOKENS when passed A=0x08 (BASIC keyword "TAB")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "TAB ".
po_tokens_0f08_setup:
  jp      fake0

po_tokens_0f08_setup_regs:
  ld      a, 0x08
  ld      iy, 0x5000
  ret

po_tokens_0f08_effects:
  ld      de, msg_po_tokens_0f08
  call    print_msg_de
  ret

po_tokens_0f08_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f08: .asciz "TAB "


# Test po_tokens_0f09 tests PO_TOKENS when passed A=0x09 (BASIC keyword "VAL$")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "VAL$ ".
po_tokens_0f09_setup:
  jp      fake0

po_tokens_0f09_setup_regs:
  ld      a, 0x09
  ld      iy, 0x5000
  ret

po_tokens_0f09_effects:
  ld      de, msg_po_tokens_0f09
  call    print_msg_de
  ret

po_tokens_0f09_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f09: .asciz "VAL$ "


# Test po_tokens_0f0a tests PO_TOKENS when passed A=0x0a (BASIC keyword "CODE")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CODE ".
po_tokens_0f0a_setup:
  jp      fake0

po_tokens_0f0a_setup_regs:
  ld      a, 0x0a
  ld      iy, 0x5000
  ret

po_tokens_0f0a_effects:
  ld      de, msg_po_tokens_0f0a
  call    print_msg_de
  ret

po_tokens_0f0a_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f0a: .asciz "CODE "


# Test po_tokens_0f0b tests PO_TOKENS when passed A=0x0b (BASIC keyword "VAL")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "VAL ".
po_tokens_0f0b_setup:
  jp      fake0

po_tokens_0f0b_setup_regs:
  ld      a, 0x0b
  ld      iy, 0x5000
  ret

po_tokens_0f0b_effects:
  ld      de, msg_po_tokens_0f0b
  call    print_msg_de
  ret

po_tokens_0f0b_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f0b: .asciz "VAL "


# Test po_tokens_0f0c tests PO_TOKENS when passed A=0x0c (BASIC keyword "LEN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LEN ".
po_tokens_0f0c_setup:
  jp      fake0

po_tokens_0f0c_setup_regs:
  ld      a, 0x0c
  ld      iy, 0x5000
  ret

po_tokens_0f0c_effects:
  ld      de, msg_po_tokens_0f0c
  call    print_msg_de
  ret

po_tokens_0f0c_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f0c: .asciz "LEN "


# Test po_tokens_0f0d tests PO_TOKENS when passed A=0x0d (BASIC keyword "SIN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "SIN ".
po_tokens_0f0d_setup:
  jp      fake0

po_tokens_0f0d_setup_regs:
  ld      a, 0x0d
  ld      iy, 0x5000
  ret

po_tokens_0f0d_effects:
  ld      de, msg_po_tokens_0f0d
  call    print_msg_de
  ret

po_tokens_0f0d_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f0d: .asciz "SIN "


# Test po_tokens_0f0e tests PO_TOKENS when passed A=0x0e (BASIC keyword "COS")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "COS ".
po_tokens_0f0e_setup:
  jp      fake0

po_tokens_0f0e_setup_regs:
  ld      a, 0x0e
  ld      iy, 0x5000
  ret

po_tokens_0f0e_effects:
  ld      de, msg_po_tokens_0f0e
  call    print_msg_de
  ret

po_tokens_0f0e_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f0e: .asciz "COS "


# Test po_tokens_0f0f tests PO_TOKENS when passed A=0x0f (BASIC keyword "TAN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "TAN ".
po_tokens_0f0f_setup:
  jp      fake0

po_tokens_0f0f_setup_regs:
  ld      a, 0x0f
  ld      iy, 0x5000
  ret

po_tokens_0f0f_effects:
  ld      de, msg_po_tokens_0f0f
  call    print_msg_de
  ret

po_tokens_0f0f_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f0f: .asciz "TAN "


# Test po_tokens_0f10 tests PO_TOKENS when passed A=0x10 (BASIC keyword "ASN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ASN ".
po_tokens_0f10_setup:
  jp      fake0

po_tokens_0f10_setup_regs:
  ld      a, 0x10
  ld      iy, 0x5000
  ret

po_tokens_0f10_effects:
  ld      de, msg_po_tokens_0f10
  call    print_msg_de
  ret

po_tokens_0f10_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f10: .asciz "ASN "


# Test po_tokens_0f11 tests PO_TOKENS when passed A=0x11 (BASIC keyword "ACS")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ACS ".
po_tokens_0f11_setup:
  jp      fake0

po_tokens_0f11_setup_regs:
  ld      a, 0x11
  ld      iy, 0x5000
  ret

po_tokens_0f11_effects:
  ld      de, msg_po_tokens_0f11
  call    print_msg_de
  ret

po_tokens_0f11_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f11: .asciz "ACS "


# Test po_tokens_0f12 tests PO_TOKENS when passed A=0x12 (BASIC keyword "ATN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ATN ".
po_tokens_0f12_setup:
  jp      fake0

po_tokens_0f12_setup_regs:
  ld      a, 0x12
  ld      iy, 0x5000
  ret

po_tokens_0f12_effects:
  ld      de, msg_po_tokens_0f12
  call    print_msg_de
  ret

po_tokens_0f12_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f12: .asciz "ATN "


# Test po_tokens_0f13 tests PO_TOKENS when passed A=0x13 (BASIC keyword "LN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LN ".
po_tokens_0f13_setup:
  jp      fake0

po_tokens_0f13_setup_regs:
  ld      a, 0x13
  ld      iy, 0x5000
  ret

po_tokens_0f13_effects:
  ld      de, msg_po_tokens_0f13
  call    print_msg_de
  ret

po_tokens_0f13_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f13: .asciz "LN "


# Test po_tokens_0f14 tests PO_TOKENS when passed A=0x14 (BASIC keyword "EXP")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "EXP ".
po_tokens_0f14_setup:
  jp      fake0

po_tokens_0f14_setup_regs:
  ld      a, 0x14
  ld      iy, 0x5000
  ret

po_tokens_0f14_effects:
  ld      de, msg_po_tokens_0f14
  call    print_msg_de
  ret

po_tokens_0f14_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f14: .asciz "EXP "


# Test po_tokens_0f15 tests PO_TOKENS when passed A=0x15 (BASIC keyword "INT")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "INT ".
po_tokens_0f15_setup:
  jp      fake0

po_tokens_0f15_setup_regs:
  ld      a, 0x15
  ld      iy, 0x5000
  ret

po_tokens_0f15_effects:
  ld      de, msg_po_tokens_0f15
  call    print_msg_de
  ret

po_tokens_0f15_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f15: .asciz "INT "


# Test po_tokens_0f16 tests PO_TOKENS when passed A=0x16 (BASIC keyword "SQR")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "SQR ".
po_tokens_0f16_setup:
  jp      fake0

po_tokens_0f16_setup_regs:
  ld      a, 0x16
  ld      iy, 0x5000
  ret

po_tokens_0f16_effects:
  ld      de, msg_po_tokens_0f16
  call    print_msg_de
  ret

po_tokens_0f16_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f16: .asciz "SQR "


# Test po_tokens_0f17 tests PO_TOKENS when passed A=0x17 (BASIC keyword "SGN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "SGN ".
po_tokens_0f17_setup:
  jp      fake0

po_tokens_0f17_setup_regs:
  ld      a, 0x17
  ld      iy, 0x5000
  ret

po_tokens_0f17_effects:
  ld      de, msg_po_tokens_0f17
  call    print_msg_de
  ret

po_tokens_0f17_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f17: .asciz "SGN "


# Test po_tokens_0f18 tests PO_TOKENS when passed A=0x18 (BASIC keyword "ABS")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ABS ".
po_tokens_0f18_setup:
  jp      fake0

po_tokens_0f18_setup_regs:
  ld      a, 0x18
  ld      iy, 0x5000
  ret

po_tokens_0f18_effects:
  ld      de, msg_po_tokens_0f18
  call    print_msg_de
  ret

po_tokens_0f18_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f18: .asciz "ABS "


# Test po_tokens_0f19 tests PO_TOKENS when passed A=0x19 (BASIC keyword "PEEK")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "PEEK ".
po_tokens_0f19_setup:
  jp      fake0

po_tokens_0f19_setup_regs:
  ld      a, 0x19
  ld      iy, 0x5000
  ret

po_tokens_0f19_effects:
  ld      de, msg_po_tokens_0f19
  call    print_msg_de
  ret

po_tokens_0f19_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f19: .asciz "PEEK "


# Test po_tokens_0f1a tests PO_TOKENS when passed A=0x1a (BASIC keyword "IN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "IN ".
po_tokens_0f1a_setup:
  jp      fake0

po_tokens_0f1a_setup_regs:
  ld      a, 0x1a
  ld      iy, 0x5000
  ret

po_tokens_0f1a_effects:
  ld      de, msg_po_tokens_0f1a
  call    print_msg_de
  ret

po_tokens_0f1a_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f1a: .asciz "IN "


# Test po_tokens_0f1b tests PO_TOKENS when passed A=0x1b (BASIC keyword "USR")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "USR ".
po_tokens_0f1b_setup:
  jp      fake0

po_tokens_0f1b_setup_regs:
  ld      a, 0x1b
  ld      iy, 0x5000
  ret

po_tokens_0f1b_effects:
  ld      de, msg_po_tokens_0f1b
  call    print_msg_de
  ret

po_tokens_0f1b_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f1b: .asciz "USR "


# Test po_tokens_0f1c tests PO_TOKENS when passed A=0x1c (BASIC keyword "STR$")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "STR$ ".
po_tokens_0f1c_setup:
  jp      fake0

po_tokens_0f1c_setup_regs:
  ld      a, 0x1c
  ld      iy, 0x5000
  ret

po_tokens_0f1c_effects:
  ld      de, msg_po_tokens_0f1c
  call    print_msg_de
  ret

po_tokens_0f1c_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f1c: .asciz "STR$ "


# Test po_tokens_0f1d tests PO_TOKENS when passed A=0x1d (BASIC keyword "CHR$")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CHR$ ".
po_tokens_0f1d_setup:
  jp      fake0

po_tokens_0f1d_setup_regs:
  ld      a, 0x1d
  ld      iy, 0x5000
  ret

po_tokens_0f1d_effects:
  ld      de, msg_po_tokens_0f1d
  call    print_msg_de
  ret

po_tokens_0f1d_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f1d: .asciz "CHR$ "


# Test po_tokens_0f1e tests PO_TOKENS when passed A=0x1e (BASIC keyword "NOT")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "NOT ".
po_tokens_0f1e_setup:
  jp      fake0

po_tokens_0f1e_setup_regs:
  ld      a, 0x1e
  ld      iy, 0x5000
  ret

po_tokens_0f1e_effects:
  ld      de, msg_po_tokens_0f1e
  call    print_msg_de
  ret

po_tokens_0f1e_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f1e: .asciz "NOT "


# Test po_tokens_0f1f tests PO_TOKENS when passed A=0x1f (BASIC keyword "BIN")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "BIN ".
po_tokens_0f1f_setup:
  jp      fake0

po_tokens_0f1f_setup_regs:
  ld      a, 0x1f
  ld      iy, 0x5000
  ret

po_tokens_0f1f_effects:
  ld      de, msg_po_tokens_0f1f
  call    print_msg_de
  ret

po_tokens_0f1f_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f1f: .asciz "BIN "


# Test po_tokens_0f20 tests PO_TOKENS when passed A=0x20 (BASIC keyword "OR")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " OR ".
po_tokens_0f20_setup:
  jp      fake0

po_tokens_0f20_setup_regs:
  ld      a, 0x20
  ld      iy, 0x5000
  ret

po_tokens_0f20_effects:
  ld      de, msg_po_tokens_0f20
  call    print_msg_de
  ret

po_tokens_0f20_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f20: .asciz " OR "


# Test po_tokens_0f21 tests PO_TOKENS when passed A=0x21 (BASIC keyword "AND")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " AND ".
po_tokens_0f21_setup:
  jp      fake0

po_tokens_0f21_setup_regs:
  ld      a, 0x21
  ld      iy, 0x5000
  ret

po_tokens_0f21_effects:
  ld      de, msg_po_tokens_0f21
  call    print_msg_de
  ret

po_tokens_0f21_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f21: .asciz " AND "


# Test po_tokens_0f22 tests PO_TOKENS when passed A=0x22 (BASIC keyword "<=")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "<=".
po_tokens_0f22_setup:
  jp      fake0

po_tokens_0f22_setup_regs:
  ld      a, 0x22
  ld      iy, 0x5000
  ret

po_tokens_0f22_effects:
  ld      de, msg_po_tokens_0f22
  call    print_msg_de
  ret

po_tokens_0f22_effects_regs:
  push    af
  pop     de
  ld      a, 0x7a
  ldf     C_FLAG|N_FLAG|PV_FLAG|S_FLAG
  ret

msg_po_tokens_0f22: .asciz "<="


# Test po_tokens_0f23 tests PO_TOKENS when passed A=0x23 (BASIC keyword ">=")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is ">=".
po_tokens_0f23_setup:
  jp      fake0

po_tokens_0f23_setup_regs:
  ld      a, 0x23
  ld      iy, 0x5000
  ret

po_tokens_0f23_effects:
  ld      de, msg_po_tokens_0f23
  call    print_msg_de
  ret

po_tokens_0f23_effects_regs:
  push    af
  pop     de
  ld      a, 0x7a
  ldf     C_FLAG|N_FLAG|PV_FLAG|S_FLAG
  ret

msg_po_tokens_0f23: .asciz ">="


# Test po_tokens_0f24 tests PO_TOKENS when passed A=0x24 (BASIC keyword "<>")
# with bit 0 of [IY+1] set to 0 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "<>".
po_tokens_0f24_setup:
  jp      fake0

po_tokens_0f24_setup_regs:
  ld      a, 0x24
  ld      iy, 0x5000
  ret

po_tokens_0f24_effects:
  ld      de, msg_po_tokens_0f24
  call    print_msg_de
  ret

po_tokens_0f24_effects_regs:
  push    af
  pop     de
  ld      a, 0x7c
  ldf     C_FLAG|N_FLAG|PV_FLAG|S_FLAG
  ret

msg_po_tokens_0f24: .asciz "<>"


# Test po_tokens_0f25 tests PO_TOKENS when passed A=0x25 (BASIC keyword "LINE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " LINE ".
po_tokens_0f25_setup:
  jp      fake0

po_tokens_0f25_setup_regs:
  ld      a, 0x25
  ld      iy, 0x5000
  ret

po_tokens_0f25_effects:
  ld      de, msg_po_tokens_0f25
  call    print_msg_de
  ret

po_tokens_0f25_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f25: .asciz " LINE "


# Test po_tokens_0f26 tests PO_TOKENS when passed A=0x26 (BASIC keyword "THEN")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " THEN ".
po_tokens_0f26_setup:
  jp      fake0

po_tokens_0f26_setup_regs:
  ld      a, 0x26
  ld      iy, 0x5000
  ret

po_tokens_0f26_effects:
  ld      de, msg_po_tokens_0f26
  call    print_msg_de
  ret

po_tokens_0f26_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f26: .asciz " THEN "


# Test po_tokens_0f27 tests PO_TOKENS when passed A=0x27 (BASIC keyword "TO")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " TO ".
po_tokens_0f27_setup:
  jp      fake0

po_tokens_0f27_setup_regs:
  ld      a, 0x27
  ld      iy, 0x5000
  ret

po_tokens_0f27_effects:
  ld      de, msg_po_tokens_0f27
  call    print_msg_de
  ret

po_tokens_0f27_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f27: .asciz " TO "


# Test po_tokens_0f28 tests PO_TOKENS when passed A=0x28 (BASIC keyword "STEP")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " STEP ".
po_tokens_0f28_setup:
  jp      fake0

po_tokens_0f28_setup_regs:
  ld      a, 0x28
  ld      iy, 0x5000
  ret

po_tokens_0f28_effects:
  ld      de, msg_po_tokens_0f28
  call    print_msg_de
  ret

po_tokens_0f28_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f28: .asciz " STEP "


# Test po_tokens_0f29 tests PO_TOKENS when passed A=0x29 (BASIC keyword "DEF FN")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " DEF FN ".
po_tokens_0f29_setup:
  jp      fake0

po_tokens_0f29_setup_regs:
  ld      a, 0x29
  ld      iy, 0x5000
  ret

po_tokens_0f29_effects:
  ld      de, msg_po_tokens_0f29
  call    print_msg_de
  ret

po_tokens_0f29_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f29: .asciz " DEF FN "


# Test po_tokens_0f2a tests PO_TOKENS when passed A=0x2a (BASIC keyword "CAT")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " CAT ".
po_tokens_0f2a_setup:
  jp      fake0

po_tokens_0f2a_setup_regs:
  ld      a, 0x2a
  ld      iy, 0x5000
  ret

po_tokens_0f2a_effects:
  ld      de, msg_po_tokens_0f2a
  call    print_msg_de
  ret

po_tokens_0f2a_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f2a: .asciz " CAT "


# Test po_tokens_0f2b tests PO_TOKENS when passed A=0x2b (BASIC keyword "FORMAT")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " FORMAT ".
po_tokens_0f2b_setup:
  jp      fake0

po_tokens_0f2b_setup_regs:
  ld      a, 0x2b
  ld      iy, 0x5000
  ret

po_tokens_0f2b_effects:
  ld      de, msg_po_tokens_0f2b
  call    print_msg_de
  ret

po_tokens_0f2b_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f2b: .asciz " FORMAT "


# Test po_tokens_0f2c tests PO_TOKENS when passed A=0x2c (BASIC keyword "MOVE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " MOVE ".
po_tokens_0f2c_setup:
  jp      fake0

po_tokens_0f2c_setup_regs:
  ld      a, 0x2c
  ld      iy, 0x5000
  ret

po_tokens_0f2c_effects:
  ld      de, msg_po_tokens_0f2c
  call    print_msg_de
  ret

po_tokens_0f2c_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f2c: .asciz " MOVE "


# Test po_tokens_0f2d tests PO_TOKENS when passed A=0x2d (BASIC keyword "ERASE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " ERASE ".
po_tokens_0f2d_setup:
  jp      fake0

po_tokens_0f2d_setup_regs:
  ld      a, 0x2d
  ld      iy, 0x5000
  ret

po_tokens_0f2d_effects:
  ld      de, msg_po_tokens_0f2d
  call    print_msg_de
  ret

po_tokens_0f2d_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f2d: .asciz " ERASE "


# Test po_tokens_0f2e tests PO_TOKENS when passed A=0x2e (BASIC keyword "OPEN #")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " OPEN #".
po_tokens_0f2e_setup:
  jp      fake0

po_tokens_0f2e_setup_regs:
  ld      a, 0x2e
  ld      iy, 0x5000
  ret

po_tokens_0f2e_effects:
  ld      de, msg_po_tokens_0f2e
  call    print_msg_de
  ret

po_tokens_0f2e_effects_regs:
  push    af
  pop     de
  ld      a, 0x46
  ldf     C_FLAG|N_FLAG|PV_FLAG|S_FLAG
  ret

msg_po_tokens_0f2e: .asciz " OPEN #"


# Test po_tokens_0f2f tests PO_TOKENS when passed A=0x2f (BASIC keyword "CLOSE #")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " CLOSE #".
po_tokens_0f2f_setup:
  jp      fake0

po_tokens_0f2f_setup_regs:
  ld      a, 0x2f
  ld      iy, 0x5000
  ret

po_tokens_0f2f_effects:
  ld      de, msg_po_tokens_0f2f
  call    print_msg_de
  ret

po_tokens_0f2f_effects_regs:
  push    af
  pop     de
  ld      a, 0x46
  ldf     C_FLAG|N_FLAG|PV_FLAG|S_FLAG
  ret

msg_po_tokens_0f2f: .asciz " CLOSE #"


# Test po_tokens_0f30 tests PO_TOKENS when passed A=0x30 (BASIC keyword "MERGE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " MERGE ".
po_tokens_0f30_setup:
  jp      fake0

po_tokens_0f30_setup_regs:
  ld      a, 0x30
  ld      iy, 0x5000
  ret

po_tokens_0f30_effects:
  ld      de, msg_po_tokens_0f30
  call    print_msg_de
  ret

po_tokens_0f30_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f30: .asciz " MERGE "


# Test po_tokens_0f31 tests PO_TOKENS when passed A=0x31 (BASIC keyword "VERIFY")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " VERIFY ".
po_tokens_0f31_setup:
  jp      fake0

po_tokens_0f31_setup_regs:
  ld      a, 0x31
  ld      iy, 0x5000
  ret

po_tokens_0f31_effects:
  ld      de, msg_po_tokens_0f31
  call    print_msg_de
  ret

po_tokens_0f31_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f31: .asciz " VERIFY "


# Test po_tokens_0f32 tests PO_TOKENS when passed A=0x32 (BASIC keyword "BEEP")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " BEEP ".
po_tokens_0f32_setup:
  jp      fake0

po_tokens_0f32_setup_regs:
  ld      a, 0x32
  ld      iy, 0x5000
  ret

po_tokens_0f32_effects:
  ld      de, msg_po_tokens_0f32
  call    print_msg_de
  ret

po_tokens_0f32_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f32: .asciz " BEEP "


# Test po_tokens_0f33 tests PO_TOKENS when passed A=0x33 (BASIC keyword "CIRCLE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " CIRCLE ".
po_tokens_0f33_setup:
  jp      fake0

po_tokens_0f33_setup_regs:
  ld      a, 0x33
  ld      iy, 0x5000
  ret

po_tokens_0f33_effects:
  ld      de, msg_po_tokens_0f33
  call    print_msg_de
  ret

po_tokens_0f33_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f33: .asciz " CIRCLE "


# Test po_tokens_0f34 tests PO_TOKENS when passed A=0x34 (BASIC keyword "INK")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " INK ".
po_tokens_0f34_setup:
  jp      fake0

po_tokens_0f34_setup_regs:
  ld      a, 0x34
  ld      iy, 0x5000
  ret

po_tokens_0f34_effects:
  ld      de, msg_po_tokens_0f34
  call    print_msg_de
  ret

po_tokens_0f34_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f34: .asciz " INK "


# Test po_tokens_0f35 tests PO_TOKENS when passed A=0x35 (BASIC keyword "PAPER")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " PAPER ".
po_tokens_0f35_setup:
  jp      fake0

po_tokens_0f35_setup_regs:
  ld      a, 0x35
  ld      iy, 0x5000
  ret

po_tokens_0f35_effects:
  ld      de, msg_po_tokens_0f35
  call    print_msg_de
  ret

po_tokens_0f35_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f35: .asciz " PAPER "


# Test po_tokens_0f36 tests PO_TOKENS when passed A=0x36 (BASIC keyword "FLASH")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " FLASH ".
po_tokens_0f36_setup:
  jp      fake0

po_tokens_0f36_setup_regs:
  ld      a, 0x36
  ld      iy, 0x5000
  ret

po_tokens_0f36_effects:
  ld      de, msg_po_tokens_0f36
  call    print_msg_de
  ret

po_tokens_0f36_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f36: .asciz " FLASH "


# Test po_tokens_0f37 tests PO_TOKENS when passed A=0x37 (BASIC keyword "BRIGHT")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " BRIGHT ".
po_tokens_0f37_setup:
  jp      fake0

po_tokens_0f37_setup_regs:
  ld      a, 0x37
  ld      iy, 0x5000
  ret

po_tokens_0f37_effects:
  ld      de, msg_po_tokens_0f37
  call    print_msg_de
  ret

po_tokens_0f37_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f37: .asciz " BRIGHT "


# Test po_tokens_0f38 tests PO_TOKENS when passed A=0x38 (BASIC keyword "INVERSE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " INVERSE ".
po_tokens_0f38_setup:
  jp      fake0

po_tokens_0f38_setup_regs:
  ld      a, 0x38
  ld      iy, 0x5000
  ret

po_tokens_0f38_effects:
  ld      de, msg_po_tokens_0f38
  call    print_msg_de
  ret

po_tokens_0f38_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f38: .asciz " INVERSE "


# Test po_tokens_0f39 tests PO_TOKENS when passed A=0x39 (BASIC keyword "OVER")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " OVER ".
po_tokens_0f39_setup:
  jp      fake0

po_tokens_0f39_setup_regs:
  ld      a, 0x39
  ld      iy, 0x5000
  ret

po_tokens_0f39_effects:
  ld      de, msg_po_tokens_0f39
  call    print_msg_de
  ret

po_tokens_0f39_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f39: .asciz " OVER "


# Test po_tokens_0f3a tests PO_TOKENS when passed A=0x3a (BASIC keyword "OUT")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " OUT ".
po_tokens_0f3a_setup:
  jp      fake0

po_tokens_0f3a_setup_regs:
  ld      a, 0x3a
  ld      iy, 0x5000
  ret

po_tokens_0f3a_effects:
  ld      de, msg_po_tokens_0f3a
  call    print_msg_de
  ret

po_tokens_0f3a_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f3a: .asciz " OUT "


# Test po_tokens_0f3b tests PO_TOKENS when passed A=0x3b (BASIC keyword "LPRINT")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " LPRINT ".
po_tokens_0f3b_setup:
  jp      fake0

po_tokens_0f3b_setup_regs:
  ld      a, 0x3b
  ld      iy, 0x5000
  ret

po_tokens_0f3b_effects:
  ld      de, msg_po_tokens_0f3b
  call    print_msg_de
  ret

po_tokens_0f3b_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f3b: .asciz " LPRINT "


# Test po_tokens_0f3c tests PO_TOKENS when passed A=0x3c (BASIC keyword "LLIST")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " LLIST ".
po_tokens_0f3c_setup:
  jp      fake0

po_tokens_0f3c_setup_regs:
  ld      a, 0x3c
  ld      iy, 0x5000
  ret

po_tokens_0f3c_effects:
  ld      de, msg_po_tokens_0f3c
  call    print_msg_de
  ret

po_tokens_0f3c_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f3c: .asciz " LLIST "


# Test po_tokens_0f3d tests PO_TOKENS when passed A=0x3d (BASIC keyword "STOP")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " STOP ".
po_tokens_0f3d_setup:
  jp      fake0

po_tokens_0f3d_setup_regs:
  ld      a, 0x3d
  ld      iy, 0x5000
  ret

po_tokens_0f3d_effects:
  ld      de, msg_po_tokens_0f3d
  call    print_msg_de
  ret

po_tokens_0f3d_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f3d: .asciz " STOP "


# Test po_tokens_0f3e tests PO_TOKENS when passed A=0x3e (BASIC keyword "READ")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " READ ".
po_tokens_0f3e_setup:
  jp      fake0

po_tokens_0f3e_setup_regs:
  ld      a, 0x3e
  ld      iy, 0x5000
  ret

po_tokens_0f3e_effects:
  ld      de, msg_po_tokens_0f3e
  call    print_msg_de
  ret

po_tokens_0f3e_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f3e: .asciz " READ "


# Test po_tokens_0f3f tests PO_TOKENS when passed A=0x3f (BASIC keyword "DATA")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " DATA ".
po_tokens_0f3f_setup:
  jp      fake0

po_tokens_0f3f_setup_regs:
  ld      a, 0x3f
  ld      iy, 0x5000
  ret

po_tokens_0f3f_effects:
  ld      de, msg_po_tokens_0f3f
  call    print_msg_de
  ret

po_tokens_0f3f_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f3f: .asciz " DATA "


# Test po_tokens_0f40 tests PO_TOKENS when passed A=0x40 (BASIC keyword "RESTORE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " RESTORE ".
po_tokens_0f40_setup:
  jp      fake0

po_tokens_0f40_setup_regs:
  ld      a, 0x40
  ld      iy, 0x5000
  ret

po_tokens_0f40_effects:
  ld      de, msg_po_tokens_0f40
  call    print_msg_de
  ret

po_tokens_0f40_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f40: .asciz " RESTORE "


# Test po_tokens_0f41 tests PO_TOKENS when passed A=0x41 (BASIC keyword "NEW")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " NEW ".
po_tokens_0f41_setup:
  jp      fake0

po_tokens_0f41_setup_regs:
  ld      a, 0x41
  ld      iy, 0x5000
  ret

po_tokens_0f41_effects:
  ld      de, msg_po_tokens_0f41
  call    print_msg_de
  ret

po_tokens_0f41_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f41: .asciz " NEW "


# Test po_tokens_0f42 tests PO_TOKENS when passed A=0x42 (BASIC keyword "BORDER")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " BORDER ".
po_tokens_0f42_setup:
  jp      fake0

po_tokens_0f42_setup_regs:
  ld      a, 0x42
  ld      iy, 0x5000
  ret

po_tokens_0f42_effects:
  ld      de, msg_po_tokens_0f42
  call    print_msg_de
  ret

po_tokens_0f42_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f42: .asciz " BORDER "


# Test po_tokens_0f43 tests PO_TOKENS when passed A=0x43 (BASIC keyword "CONTINUE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " CONTINUE ".
po_tokens_0f43_setup:
  jp      fake0

po_tokens_0f43_setup_regs:
  ld      a, 0x43
  ld      iy, 0x5000
  ret

po_tokens_0f43_effects:
  ld      de, msg_po_tokens_0f43
  call    print_msg_de
  ret

po_tokens_0f43_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f43: .asciz " CONTINUE "


# Test po_tokens_0f44 tests PO_TOKENS when passed A=0x44 (BASIC keyword "DIM")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " DIM ".
po_tokens_0f44_setup:
  jp      fake0

po_tokens_0f44_setup_regs:
  ld      a, 0x44
  ld      iy, 0x5000
  ret

po_tokens_0f44_effects:
  ld      de, msg_po_tokens_0f44
  call    print_msg_de
  ret

po_tokens_0f44_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f44: .asciz " DIM "


# Test po_tokens_0f45 tests PO_TOKENS when passed A=0x45 (BASIC keyword "REM")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " REM ".
po_tokens_0f45_setup:
  jp      fake0

po_tokens_0f45_setup_regs:
  ld      a, 0x45
  ld      iy, 0x5000
  ret

po_tokens_0f45_effects:
  ld      de, msg_po_tokens_0f45
  call    print_msg_de
  ret

po_tokens_0f45_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f45: .asciz " REM "


# Test po_tokens_0f46 tests PO_TOKENS when passed A=0x46 (BASIC keyword "FOR")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " FOR ".
po_tokens_0f46_setup:
  jp      fake0

po_tokens_0f46_setup_regs:
  ld      a, 0x46
  ld      iy, 0x5000
  ret

po_tokens_0f46_effects:
  ld      de, msg_po_tokens_0f46
  call    print_msg_de
  ret

po_tokens_0f46_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f46: .asciz " FOR "


# Test po_tokens_0f47 tests PO_TOKENS when passed A=0x47 (BASIC keyword "GO TO")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " GO TO ".
po_tokens_0f47_setup:
  jp      fake0

po_tokens_0f47_setup_regs:
  ld      a, 0x47
  ld      iy, 0x5000
  ret

po_tokens_0f47_effects:
  ld      de, msg_po_tokens_0f47
  call    print_msg_de
  ret

po_tokens_0f47_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f47: .asciz " GO TO "


# Test po_tokens_0f48 tests PO_TOKENS when passed A=0x48 (BASIC keyword "GO SUB")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " GO SUB ".
po_tokens_0f48_setup:
  jp      fake0

po_tokens_0f48_setup_regs:
  ld      a, 0x48
  ld      iy, 0x5000
  ret

po_tokens_0f48_effects:
  ld      de, msg_po_tokens_0f48
  call    print_msg_de
  ret

po_tokens_0f48_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f48: .asciz " GO SUB "


# Test po_tokens_0f49 tests PO_TOKENS when passed A=0x49 (BASIC keyword "INPUT")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " INPUT ".
po_tokens_0f49_setup:
  jp      fake0

po_tokens_0f49_setup_regs:
  ld      a, 0x49
  ld      iy, 0x5000
  ret

po_tokens_0f49_effects:
  ld      de, msg_po_tokens_0f49
  call    print_msg_de
  ret

po_tokens_0f49_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f49: .asciz " INPUT "


# Test po_tokens_0f4a tests PO_TOKENS when passed A=0x4a (BASIC keyword "LOAD")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " LOAD ".
po_tokens_0f4a_setup:
  jp      fake0

po_tokens_0f4a_setup_regs:
  ld      a, 0x4a
  ld      iy, 0x5000
  ret

po_tokens_0f4a_effects:
  ld      de, msg_po_tokens_0f4a
  call    print_msg_de
  ret

po_tokens_0f4a_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f4a: .asciz " LOAD "


# Test po_tokens_0f4b tests PO_TOKENS when passed A=0x4b (BASIC keyword "LIST")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " LIST ".
po_tokens_0f4b_setup:
  jp      fake0

po_tokens_0f4b_setup_regs:
  ld      a, 0x4b
  ld      iy, 0x5000
  ret

po_tokens_0f4b_effects:
  ld      de, msg_po_tokens_0f4b
  call    print_msg_de
  ret

po_tokens_0f4b_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f4b: .asciz " LIST "


# Test po_tokens_0f4c tests PO_TOKENS when passed A=0x4c (BASIC keyword "LET")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " LET ".
po_tokens_0f4c_setup:
  jp      fake0

po_tokens_0f4c_setup_regs:
  ld      a, 0x4c
  ld      iy, 0x5000
  ret

po_tokens_0f4c_effects:
  ld      de, msg_po_tokens_0f4c
  call    print_msg_de
  ret

po_tokens_0f4c_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f4c: .asciz " LET "


# Test po_tokens_0f4d tests PO_TOKENS when passed A=0x4d (BASIC keyword "PAUSE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " PAUSE ".
po_tokens_0f4d_setup:
  jp      fake0

po_tokens_0f4d_setup_regs:
  ld      a, 0x4d
  ld      iy, 0x5000
  ret

po_tokens_0f4d_effects:
  ld      de, msg_po_tokens_0f4d
  call    print_msg_de
  ret

po_tokens_0f4d_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f4d: .asciz " PAUSE "


# Test po_tokens_0f4e tests PO_TOKENS when passed A=0x4e (BASIC keyword "NEXT")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " NEXT ".
po_tokens_0f4e_setup:
  jp      fake0

po_tokens_0f4e_setup_regs:
  ld      a, 0x4e
  ld      iy, 0x5000
  ret

po_tokens_0f4e_effects:
  ld      de, msg_po_tokens_0f4e
  call    print_msg_de
  ret

po_tokens_0f4e_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f4e: .asciz " NEXT "


# Test po_tokens_0f4f tests PO_TOKENS when passed A=0x4f (BASIC keyword "POKE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " POKE ".
po_tokens_0f4f_setup:
  jp      fake0

po_tokens_0f4f_setup_regs:
  ld      a, 0x4f
  ld      iy, 0x5000
  ret

po_tokens_0f4f_effects:
  ld      de, msg_po_tokens_0f4f
  call    print_msg_de
  ret

po_tokens_0f4f_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f4f: .asciz " POKE "


# Test po_tokens_0f50 tests PO_TOKENS when passed A=0x50 (BASIC keyword "PRINT")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " PRINT ".
po_tokens_0f50_setup:
  jp      fake0

po_tokens_0f50_setup_regs:
  ld      a, 0x50
  ld      iy, 0x5000
  ret

po_tokens_0f50_effects:
  ld      de, msg_po_tokens_0f50
  call    print_msg_de
  ret

po_tokens_0f50_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f50: .asciz " PRINT "


# Test po_tokens_0f51 tests PO_TOKENS when passed A=0x51 (BASIC keyword "PLOT")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " PLOT ".
po_tokens_0f51_setup:
  jp      fake0

po_tokens_0f51_setup_regs:
  ld      a, 0x51
  ld      iy, 0x5000
  ret

po_tokens_0f51_effects:
  ld      de, msg_po_tokens_0f51
  call    print_msg_de
  ret

po_tokens_0f51_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f51: .asciz " PLOT "


# Test po_tokens_0f52 tests PO_TOKENS when passed A=0x52 (BASIC keyword "RUN")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " RUN ".
po_tokens_0f52_setup:
  jp      fake0

po_tokens_0f52_setup_regs:
  ld      a, 0x52
  ld      iy, 0x5000
  ret

po_tokens_0f52_effects:
  ld      de, msg_po_tokens_0f52
  call    print_msg_de
  ret

po_tokens_0f52_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG|H_FLAG
  ret

msg_po_tokens_0f52: .asciz " RUN "


# Test po_tokens_0f53 tests PO_TOKENS when passed A=0x53 (BASIC keyword "SAVE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " SAVE ".
po_tokens_0f53_setup:
  jp      fake0

po_tokens_0f53_setup_regs:
  ld      a, 0x53
  ld      iy, 0x5000
  ret

po_tokens_0f53_effects:
  ld      de, msg_po_tokens_0f53
  call    print_msg_de
  ret

po_tokens_0f53_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f53: .asciz " SAVE "


# Test po_tokens_0f54 tests PO_TOKENS when passed A=0x54 (BASIC keyword "RANDOMIZE")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " RANDOMIZE ".
po_tokens_0f54_setup:
  jp      fake0

po_tokens_0f54_setup_regs:
  ld      a, 0x54
  ld      iy, 0x5000
  ret

po_tokens_0f54_effects:
  ld      de, msg_po_tokens_0f54
  call    print_msg_de
  ret

po_tokens_0f54_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f54: .asciz " RANDOMIZE "


# Test po_tokens_0f55 tests PO_TOKENS when passed A=0x55 (BASIC keyword "IF")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " IF ".
po_tokens_0f55_setup:
  jp      fake0

po_tokens_0f55_setup_regs:
  ld      a, 0x55
  ld      iy, 0x5000
  ret

po_tokens_0f55_effects:
  ld      de, msg_po_tokens_0f55
  call    print_msg_de
  ret

po_tokens_0f55_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f55: .asciz " IF "


# Test po_tokens_0f56 tests PO_TOKENS when passed A=0x56 (BASIC keyword "CLS")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " CLS ".
po_tokens_0f56_setup:
  jp      fake0

po_tokens_0f56_setup_regs:
  ld      a, 0x56
  ld      iy, 0x5000
  ret

po_tokens_0f56_effects:
  ld      de, msg_po_tokens_0f56
  call    print_msg_de
  ret

po_tokens_0f56_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f56: .asciz " CLS "


# Test po_tokens_0f57 tests PO_TOKENS when passed A=0x57 (BASIC keyword "DRAW")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " DRAW ".
po_tokens_0f57_setup:
  jp      fake0

po_tokens_0f57_setup_regs:
  ld      a, 0x57
  ld      iy, 0x5000
  ret

po_tokens_0f57_effects:
  ld      de, msg_po_tokens_0f57
  call    print_msg_de
  ret

po_tokens_0f57_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f57: .asciz " DRAW "


# Test po_tokens_0f58 tests PO_TOKENS when passed A=0x58 (BASIC keyword "CLEAR")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " CLEAR ".
po_tokens_0f58_setup:
  jp      fake0

po_tokens_0f58_setup_regs:
  ld      a, 0x58
  ld      iy, 0x5000
  ret

po_tokens_0f58_effects:
  ld      de, msg_po_tokens_0f58
  call    print_msg_de
  ret

po_tokens_0f58_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f58: .asciz " CLEAR "


# Test po_tokens_0f59 tests PO_TOKENS when passed A=0x59 (BASIC keyword "RETURN")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " RETURN ".
po_tokens_0f59_setup:
  jp      fake0

po_tokens_0f59_setup_regs:
  ld      a, 0x59
  ld      iy, 0x5000
  ret

po_tokens_0f59_effects:
  ld      de, msg_po_tokens_0f59
  call    print_msg_de
  ret

po_tokens_0f59_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f59: .asciz " RETURN "


# Test po_tokens_0f5a tests PO_TOKENS when passed A=0x5a (BASIC keyword "COPY")
# with bit 0 of [IY+1] set to 0 (leading space _not_ suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is " COPY ".
po_tokens_0f5a_setup:
  jp      fake0

po_tokens_0f5a_setup_regs:
  ld      a, 0x5a
  ld      iy, 0x5000
  ret

po_tokens_0f5a_effects:
  ld      de, msg_po_tokens_0f5a
  call    print_msg_de
  ret

po_tokens_0f5a_effects_regs:
  push    af
  pop     de
  ld      a, ' '
  ldf     N_FLAG
  ret

msg_po_tokens_0f5a: .asciz " COPY "
