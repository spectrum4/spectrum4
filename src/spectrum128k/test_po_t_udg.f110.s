# This file is part of the Spectrum +4 Project.
# Licencing information can be found in the LICENCE file
# (C) 2021 Spectrum +4 Authors. All rights reserved.

# This file is auto-generated by test_po_t_udg.sh. DO NOT EDIT!

.text




# Test po_t_udg_a5_f110 tests PO_T_UDG when passed A=0xa5 (BASIC keyword "RND")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "RND".
po_t_udg_a5_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_a5_f110_setup_regs:
  ld      a, 0xa5
  ret

po_t_udg_a5_f110_effects:
  ld      de, msg_po_t_udg_a5_f110
  call    print_msg_de
  ret

po_t_udg_a5_f110_effects_regs:
  ld      a, 0x00                         ; entry A SUB 165 (0xa5)
  ldf     H_FLAG|X3_FLAG|C_FLAG
  ld      d, 0x00
  ld      e, N_FLAG|Z_FLAG                ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_a5_f110: .asciz "RND"


# Test po_t_udg_a6_f110 tests PO_T_UDG when passed A=0xa6 (BASIC keyword "INKEY$")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "INKEY$".
po_t_udg_a6_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_a6_f110_setup_regs:
  ld      a, 0xa6
  ret

po_t_udg_a6_f110_effects:
  ld      de, msg_po_t_udg_a6_f110
  call    print_msg_de
  ret

po_t_udg_a6_f110_effects_regs:
  ld      a, 0x01                         ; entry A SUB 165 (0xa5)
  ldf     H_FLAG|X3_FLAG|C_FLAG
  ld      d, 0x01
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_a6_f110: .asciz "INKEY$"


# Test po_t_udg_a7_f110 tests PO_T_UDG when passed A=0xa7 (BASIC keyword "PI")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "PI".
po_t_udg_a7_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_a7_f110_setup_regs:
  ld      a, 0xa7
  ret

po_t_udg_a7_f110_effects:
  ld      de, msg_po_t_udg_a7_f110
  call    print_msg_de
  ret

po_t_udg_a7_f110_effects_regs:
  ld      a, 0x02                         ; entry A SUB 165 (0xa5)
  ldf     H_FLAG|X3_FLAG|C_FLAG
  ld      d, 0x02
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_a7_f110: .asciz "PI"


# Test po_t_udg_a8_f110 tests PO_T_UDG when passed A=0xa8 (BASIC keyword "FN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "FN ".
po_t_udg_a8_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_a8_f110_setup_regs:
  ld      a, 0xa8
  ret

po_t_udg_a8_f110_effects:
  ld      de, msg_po_t_udg_a8_f110
  call    print_msg_de
  ret

po_t_udg_a8_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x03
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_a8_f110: .asciz "FN "


# Test po_t_udg_a9_f110 tests PO_T_UDG when passed A=0xa9 (BASIC keyword "POINT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "POINT ".
po_t_udg_a9_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_a9_f110_setup_regs:
  ld      a, 0xa9
  ret

po_t_udg_a9_f110_effects:
  ld      de, msg_po_t_udg_a9_f110
  call    print_msg_de
  ret

po_t_udg_a9_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x04
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_a9_f110: .asciz "POINT "


# Test po_t_udg_aa_f110 tests PO_T_UDG when passed A=0xaa (BASIC keyword "SCREEN$")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "SCREEN$ ".
po_t_udg_aa_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_aa_f110_setup_regs:
  ld      a, 0xaa
  ret

po_t_udg_aa_f110_effects:
  ld      de, msg_po_t_udg_aa_f110
  call    print_msg_de
  ret

po_t_udg_aa_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x05
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_aa_f110: .asciz "SCREEN$ "


# Test po_t_udg_ab_f110 tests PO_T_UDG when passed A=0xab (BASIC keyword "ATTR")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ATTR ".
po_t_udg_ab_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ab_f110_setup_regs:
  ld      a, 0xab
  ret

po_t_udg_ab_f110_effects:
  ld      de, msg_po_t_udg_ab_f110
  call    print_msg_de
  ret

po_t_udg_ab_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x06
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ab_f110: .asciz "ATTR "


# Test po_t_udg_ac_f110 tests PO_T_UDG when passed A=0xac (BASIC keyword "AT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "AT ".
po_t_udg_ac_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ac_f110_setup_regs:
  ld      a, 0xac
  ret

po_t_udg_ac_f110_effects:
  ld      de, msg_po_t_udg_ac_f110
  call    print_msg_de
  ret

po_t_udg_ac_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x07
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ac_f110: .asciz "AT "


# Test po_t_udg_ad_f110 tests PO_T_UDG when passed A=0xad (BASIC keyword "TAB")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "TAB ".
po_t_udg_ad_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ad_f110_setup_regs:
  ld      a, 0xad
  ret

po_t_udg_ad_f110_effects:
  ld      de, msg_po_t_udg_ad_f110
  call    print_msg_de
  ret

po_t_udg_ad_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x08
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ad_f110: .asciz "TAB "


# Test po_t_udg_ae_f110 tests PO_T_UDG when passed A=0xae (BASIC keyword "VAL$")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "VAL$ ".
po_t_udg_ae_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ae_f110_setup_regs:
  ld      a, 0xae
  ret

po_t_udg_ae_f110_effects:
  ld      de, msg_po_t_udg_ae_f110
  call    print_msg_de
  ret

po_t_udg_ae_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x09
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ae_f110: .asciz "VAL$ "


# Test po_t_udg_af_f110 tests PO_T_UDG when passed A=0xaf (BASIC keyword "CODE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CODE ".
po_t_udg_af_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_af_f110_setup_regs:
  ld      a, 0xaf
  ret

po_t_udg_af_f110_effects:
  ld      de, msg_po_t_udg_af_f110
  call    print_msg_de
  ret

po_t_udg_af_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x0a
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_af_f110: .asciz "CODE "


# Test po_t_udg_b0_f110 tests PO_T_UDG when passed A=0xb0 (BASIC keyword "VAL")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "VAL ".
po_t_udg_b0_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b0_f110_setup_regs:
  ld      a, 0xb0
  ret

po_t_udg_b0_f110_effects:
  ld      de, msg_po_t_udg_b0_f110
  call    print_msg_de
  ret

po_t_udg_b0_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x0b
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b0_f110: .asciz "VAL "


# Test po_t_udg_b1_f110 tests PO_T_UDG when passed A=0xb1 (BASIC keyword "LEN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LEN ".
po_t_udg_b1_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b1_f110_setup_regs:
  ld      a, 0xb1
  ret

po_t_udg_b1_f110_effects:
  ld      de, msg_po_t_udg_b1_f110
  call    print_msg_de
  ret

po_t_udg_b1_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x0c
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b1_f110: .asciz "LEN "


# Test po_t_udg_b2_f110 tests PO_T_UDG when passed A=0xb2 (BASIC keyword "SIN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "SIN ".
po_t_udg_b2_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b2_f110_setup_regs:
  ld      a, 0xb2
  ret

po_t_udg_b2_f110_effects:
  ld      de, msg_po_t_udg_b2_f110
  call    print_msg_de
  ret

po_t_udg_b2_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x0d
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b2_f110: .asciz "SIN "


# Test po_t_udg_b3_f110 tests PO_T_UDG when passed A=0xb3 (BASIC keyword "COS")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "COS ".
po_t_udg_b3_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b3_f110_setup_regs:
  ld      a, 0xb3
  ret

po_t_udg_b3_f110_effects:
  ld      de, msg_po_t_udg_b3_f110
  call    print_msg_de
  ret

po_t_udg_b3_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x0e
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b3_f110: .asciz "COS "


# Test po_t_udg_b4_f110 tests PO_T_UDG when passed A=0xb4 (BASIC keyword "TAN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "TAN ".
po_t_udg_b4_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b4_f110_setup_regs:
  ld      a, 0xb4
  ret

po_t_udg_b4_f110_effects:
  ld      de, msg_po_t_udg_b4_f110
  call    print_msg_de
  ret

po_t_udg_b4_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x0f
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b4_f110: .asciz "TAN "


# Test po_t_udg_b5_f110 tests PO_T_UDG when passed A=0xb5 (BASIC keyword "ASN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ASN ".
po_t_udg_b5_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b5_f110_setup_regs:
  ld      a, 0xb5
  ret

po_t_udg_b5_f110_effects:
  ld      de, msg_po_t_udg_b5_f110
  call    print_msg_de
  ret

po_t_udg_b5_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x10
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b5_f110: .asciz "ASN "


# Test po_t_udg_b6_f110 tests PO_T_UDG when passed A=0xb6 (BASIC keyword "ACS")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ACS ".
po_t_udg_b6_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b6_f110_setup_regs:
  ld      a, 0xb6
  ret

po_t_udg_b6_f110_effects:
  ld      de, msg_po_t_udg_b6_f110
  call    print_msg_de
  ret

po_t_udg_b6_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x11
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b6_f110: .asciz "ACS "


# Test po_t_udg_b7_f110 tests PO_T_UDG when passed A=0xb7 (BASIC keyword "ATN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ATN ".
po_t_udg_b7_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b7_f110_setup_regs:
  ld      a, 0xb7
  ret

po_t_udg_b7_f110_effects:
  ld      de, msg_po_t_udg_b7_f110
  call    print_msg_de
  ret

po_t_udg_b7_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x12
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b7_f110: .asciz "ATN "


# Test po_t_udg_b8_f110 tests PO_T_UDG when passed A=0xb8 (BASIC keyword "LN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LN ".
po_t_udg_b8_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b8_f110_setup_regs:
  ld      a, 0xb8
  ret

po_t_udg_b8_f110_effects:
  ld      de, msg_po_t_udg_b8_f110
  call    print_msg_de
  ret

po_t_udg_b8_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x13
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b8_f110: .asciz "LN "


# Test po_t_udg_b9_f110 tests PO_T_UDG when passed A=0xb9 (BASIC keyword "EXP")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "EXP ".
po_t_udg_b9_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_b9_f110_setup_regs:
  ld      a, 0xb9
  ret

po_t_udg_b9_f110_effects:
  ld      de, msg_po_t_udg_b9_f110
  call    print_msg_de
  ret

po_t_udg_b9_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x14
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_b9_f110: .asciz "EXP "


# Test po_t_udg_ba_f110 tests PO_T_UDG when passed A=0xba (BASIC keyword "INT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "INT ".
po_t_udg_ba_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ba_f110_setup_regs:
  ld      a, 0xba
  ret

po_t_udg_ba_f110_effects:
  ld      de, msg_po_t_udg_ba_f110
  call    print_msg_de
  ret

po_t_udg_ba_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x15
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ba_f110: .asciz "INT "


# Test po_t_udg_bb_f110 tests PO_T_UDG when passed A=0xbb (BASIC keyword "SQR")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "SQR ".
po_t_udg_bb_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_bb_f110_setup_regs:
  ld      a, 0xbb
  ret

po_t_udg_bb_f110_effects:
  ld      de, msg_po_t_udg_bb_f110
  call    print_msg_de
  ret

po_t_udg_bb_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x16
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_bb_f110: .asciz "SQR "


# Test po_t_udg_bc_f110 tests PO_T_UDG when passed A=0xbc (BASIC keyword "SGN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "SGN ".
po_t_udg_bc_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_bc_f110_setup_regs:
  ld      a, 0xbc
  ret

po_t_udg_bc_f110_effects:
  ld      de, msg_po_t_udg_bc_f110
  call    print_msg_de
  ret

po_t_udg_bc_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x17
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_bc_f110: .asciz "SGN "


# Test po_t_udg_bd_f110 tests PO_T_UDG when passed A=0xbd (BASIC keyword "ABS")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ABS ".
po_t_udg_bd_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_bd_f110_setup_regs:
  ld      a, 0xbd
  ret

po_t_udg_bd_f110_effects:
  ld      de, msg_po_t_udg_bd_f110
  call    print_msg_de
  ret

po_t_udg_bd_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x18
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_bd_f110: .asciz "ABS "


# Test po_t_udg_be_f110 tests PO_T_UDG when passed A=0xbe (BASIC keyword "PEEK")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "PEEK ".
po_t_udg_be_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_be_f110_setup_regs:
  ld      a, 0xbe
  ret

po_t_udg_be_f110_effects:
  ld      de, msg_po_t_udg_be_f110
  call    print_msg_de
  ret

po_t_udg_be_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x19
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_be_f110: .asciz "PEEK "


# Test po_t_udg_bf_f110 tests PO_T_UDG when passed A=0xbf (BASIC keyword "IN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "IN ".
po_t_udg_bf_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_bf_f110_setup_regs:
  ld      a, 0xbf
  ret

po_t_udg_bf_f110_effects:
  ld      de, msg_po_t_udg_bf_f110
  call    print_msg_de
  ret

po_t_udg_bf_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x1a
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_bf_f110: .asciz "IN "


# Test po_t_udg_c0_f110 tests PO_T_UDG when passed A=0xc0 (BASIC keyword "USR")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "USR ".
po_t_udg_c0_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c0_f110_setup_regs:
  ld      a, 0xc0
  ret

po_t_udg_c0_f110_effects:
  ld      de, msg_po_t_udg_c0_f110
  call    print_msg_de
  ret

po_t_udg_c0_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x1b
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c0_f110: .asciz "USR "


# Test po_t_udg_c1_f110 tests PO_T_UDG when passed A=0xc1 (BASIC keyword "STR$")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "STR$ ".
po_t_udg_c1_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c1_f110_setup_regs:
  ld      a, 0xc1
  ret

po_t_udg_c1_f110_effects:
  ld      de, msg_po_t_udg_c1_f110
  call    print_msg_de
  ret

po_t_udg_c1_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x1c
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c1_f110: .asciz "STR$ "


# Test po_t_udg_c2_f110 tests PO_T_UDG when passed A=0xc2 (BASIC keyword "CHR$")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CHR$ ".
po_t_udg_c2_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c2_f110_setup_regs:
  ld      a, 0xc2
  ret

po_t_udg_c2_f110_effects:
  ld      de, msg_po_t_udg_c2_f110
  call    print_msg_de
  ret

po_t_udg_c2_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x1d
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c2_f110: .asciz "CHR$ "


# Test po_t_udg_c3_f110 tests PO_T_UDG when passed A=0xc3 (BASIC keyword "NOT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "NOT ".
po_t_udg_c3_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c3_f110_setup_regs:
  ld      a, 0xc3
  ret

po_t_udg_c3_f110_effects:
  ld      de, msg_po_t_udg_c3_f110
  call    print_msg_de
  ret

po_t_udg_c3_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x1e
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c3_f110: .asciz "NOT "


# Test po_t_udg_c4_f110 tests PO_T_UDG when passed A=0xc4 (BASIC keyword "BIN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "BIN ".
po_t_udg_c4_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c4_f110_setup_regs:
  ld      a, 0xc4
  ret

po_t_udg_c4_f110_effects:
  ld      de, msg_po_t_udg_c4_f110
  call    print_msg_de
  ret

po_t_udg_c4_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x1f
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c4_f110: .asciz "BIN "


# Test po_t_udg_c5_f110 tests PO_T_UDG when passed A=0xc5 (BASIC keyword "OR")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "OR ".
po_t_udg_c5_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c5_f110_setup_regs:
  ld      a, 0xc5
  ret

po_t_udg_c5_f110_effects:
  ld      de, msg_po_t_udg_c5_f110
  call    print_msg_de
  ret

po_t_udg_c5_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x20
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c5_f110: .asciz "OR "


# Test po_t_udg_c6_f110 tests PO_T_UDG when passed A=0xc6 (BASIC keyword "AND")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "AND ".
po_t_udg_c6_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c6_f110_setup_regs:
  ld      a, 0xc6
  ret

po_t_udg_c6_f110_effects:
  ld      de, msg_po_t_udg_c6_f110
  call    print_msg_de
  ret

po_t_udg_c6_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x21
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c6_f110: .asciz "AND "


# Test po_t_udg_c7_f110 tests PO_T_UDG when passed A=0xc7 (BASIC keyword "<=")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "<=".
po_t_udg_c7_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c7_f110_setup_regs:
  ld      a, 0xc7
  ret

po_t_udg_c7_f110_effects:
  ld      de, msg_po_t_udg_c7_f110
  call    print_msg_de
  ret

po_t_udg_c7_f110_effects_regs:
  ld      a, 0x7a
  ldf     H_FLAG|X3_FLAG|C_FLAG
  ld      d, 0x22
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c7_f110: .asciz "<="


# Test po_t_udg_c8_f110 tests PO_T_UDG when passed A=0xc8 (BASIC keyword ">=")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is ">=".
po_t_udg_c8_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c8_f110_setup_regs:
  ld      a, 0xc8
  ret

po_t_udg_c8_f110_effects:
  ld      de, msg_po_t_udg_c8_f110
  call    print_msg_de
  ret

po_t_udg_c8_f110_effects_regs:
  ld      a, 0x7a
  ldf     H_FLAG|X3_FLAG|C_FLAG
  ld      d, 0x23
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c8_f110: .asciz ">="


# Test po_t_udg_c9_f110 tests PO_T_UDG when passed A=0xc9 (BASIC keyword "<>")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "<>".
po_t_udg_c9_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_c9_f110_setup_regs:
  ld      a, 0xc9
  ret

po_t_udg_c9_f110_effects:
  ld      de, msg_po_t_udg_c9_f110
  call    print_msg_de
  ret

po_t_udg_c9_f110_effects_regs:
  ld      a, 0x7c
  ldf     H_FLAG|X3_FLAG|C_FLAG
  ld      d, 0x24
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_c9_f110: .asciz "<>"


# Test po_t_udg_ca_f110 tests PO_T_UDG when passed A=0xca (BASIC keyword "LINE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LINE ".
po_t_udg_ca_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ca_f110_setup_regs:
  ld      a, 0xca
  ret

po_t_udg_ca_f110_effects:
  ld      de, msg_po_t_udg_ca_f110
  call    print_msg_de
  ret

po_t_udg_ca_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x25
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ca_f110: .asciz "LINE "


# Test po_t_udg_cb_f110 tests PO_T_UDG when passed A=0xcb (BASIC keyword "THEN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "THEN ".
po_t_udg_cb_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_cb_f110_setup_regs:
  ld      a, 0xcb
  ret

po_t_udg_cb_f110_effects:
  ld      de, msg_po_t_udg_cb_f110
  call    print_msg_de
  ret

po_t_udg_cb_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x26
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_cb_f110: .asciz "THEN "


# Test po_t_udg_cc_f110 tests PO_T_UDG when passed A=0xcc (BASIC keyword "TO")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "TO ".
po_t_udg_cc_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_cc_f110_setup_regs:
  ld      a, 0xcc
  ret

po_t_udg_cc_f110_effects:
  ld      de, msg_po_t_udg_cc_f110
  call    print_msg_de
  ret

po_t_udg_cc_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x27
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_cc_f110: .asciz "TO "


# Test po_t_udg_cd_f110 tests PO_T_UDG when passed A=0xcd (BASIC keyword "STEP")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "STEP ".
po_t_udg_cd_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_cd_f110_setup_regs:
  ld      a, 0xcd
  ret

po_t_udg_cd_f110_effects:
  ld      de, msg_po_t_udg_cd_f110
  call    print_msg_de
  ret

po_t_udg_cd_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x28
  ld      e, N_FLAG|X3_FLAG|X5_FLAG       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_cd_f110: .asciz "STEP "


# Test po_t_udg_ce_f110 tests PO_T_UDG when passed A=0xce (BASIC keyword "DEF FN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "DEF FN ".
po_t_udg_ce_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ce_f110_setup_regs:
  ld      a, 0xce
  ret

po_t_udg_ce_f110_effects:
  ld      de, msg_po_t_udg_ce_f110
  call    print_msg_de
  ret

po_t_udg_ce_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x29
  ld      e, N_FLAG|X3_FLAG|X5_FLAG       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ce_f110: .asciz "DEF FN "


# Test po_t_udg_cf_f110 tests PO_T_UDG when passed A=0xcf (BASIC keyword "CAT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CAT ".
po_t_udg_cf_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_cf_f110_setup_regs:
  ld      a, 0xcf
  ret

po_t_udg_cf_f110_effects:
  ld      de, msg_po_t_udg_cf_f110
  call    print_msg_de
  ret

po_t_udg_cf_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x2a
  ld      e, N_FLAG|X3_FLAG|X5_FLAG       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_cf_f110: .asciz "CAT "


# Test po_t_udg_d0_f110 tests PO_T_UDG when passed A=0xd0 (BASIC keyword "FORMAT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "FORMAT ".
po_t_udg_d0_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d0_f110_setup_regs:
  ld      a, 0xd0
  ret

po_t_udg_d0_f110_effects:
  ld      de, msg_po_t_udg_d0_f110
  call    print_msg_de
  ret

po_t_udg_d0_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x2b
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d0_f110: .asciz "FORMAT "


# Test po_t_udg_d1_f110 tests PO_T_UDG when passed A=0xd1 (BASIC keyword "MOVE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "MOVE ".
po_t_udg_d1_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d1_f110_setup_regs:
  ld      a, 0xd1
  ret

po_t_udg_d1_f110_effects:
  ld      de, msg_po_t_udg_d1_f110
  call    print_msg_de
  ret

po_t_udg_d1_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x2c
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d1_f110: .asciz "MOVE "


# Test po_t_udg_d2_f110 tests PO_T_UDG when passed A=0xd2 (BASIC keyword "ERASE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "ERASE ".
po_t_udg_d2_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d2_f110_setup_regs:
  ld      a, 0xd2
  ret

po_t_udg_d2_f110_effects:
  ld      de, msg_po_t_udg_d2_f110
  call    print_msg_de
  ret

po_t_udg_d2_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x2d
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d2_f110: .asciz "ERASE "


# Test po_t_udg_d3_f110 tests PO_T_UDG when passed A=0xd3 (BASIC keyword "OPEN #")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "OPEN #".
po_t_udg_d3_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d3_f110_setup_regs:
  ld      a, 0xd3
  ret

po_t_udg_d3_f110_effects:
  ld      de, msg_po_t_udg_d3_f110
  call    print_msg_de
  ret

po_t_udg_d3_f110_effects_regs:
  ld      a, 0x46
  ldf     H_FLAG|X3_FLAG|C_FLAG
  ld      d, 0x2e
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d3_f110: .asciz "OPEN #"


# Test po_t_udg_d4_f110 tests PO_T_UDG when passed A=0xd4 (BASIC keyword "CLOSE #")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CLOSE #".
po_t_udg_d4_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d4_f110_setup_regs:
  ld      a, 0xd4
  ret

po_t_udg_d4_f110_effects:
  ld      de, msg_po_t_udg_d4_f110
  call    print_msg_de
  ret

po_t_udg_d4_f110_effects_regs:
  ld      a, 0x46
  ldf     H_FLAG|X3_FLAG|C_FLAG
  ld      d, 0x2f
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d4_f110: .asciz "CLOSE #"


# Test po_t_udg_d5_f110 tests PO_T_UDG when passed A=0xd5 (BASIC keyword "MERGE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "MERGE ".
po_t_udg_d5_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d5_f110_setup_regs:
  ld      a, 0xd5
  ret

po_t_udg_d5_f110_effects:
  ld      de, msg_po_t_udg_d5_f110
  call    print_msg_de
  ret

po_t_udg_d5_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x30
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d5_f110: .asciz "MERGE "


# Test po_t_udg_d6_f110 tests PO_T_UDG when passed A=0xd6 (BASIC keyword "VERIFY")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "VERIFY ".
po_t_udg_d6_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d6_f110_setup_regs:
  ld      a, 0xd6
  ret

po_t_udg_d6_f110_effects:
  ld      de, msg_po_t_udg_d6_f110
  call    print_msg_de
  ret

po_t_udg_d6_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x31
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d6_f110: .asciz "VERIFY "


# Test po_t_udg_d7_f110 tests PO_T_UDG when passed A=0xd7 (BASIC keyword "BEEP")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "BEEP ".
po_t_udg_d7_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d7_f110_setup_regs:
  ld      a, 0xd7
  ret

po_t_udg_d7_f110_effects:
  ld      de, msg_po_t_udg_d7_f110
  call    print_msg_de
  ret

po_t_udg_d7_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x32
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d7_f110: .asciz "BEEP "


# Test po_t_udg_d8_f110 tests PO_T_UDG when passed A=0xd8 (BASIC keyword "CIRCLE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CIRCLE ".
po_t_udg_d8_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d8_f110_setup_regs:
  ld      a, 0xd8
  ret

po_t_udg_d8_f110_effects:
  ld      de, msg_po_t_udg_d8_f110
  call    print_msg_de
  ret

po_t_udg_d8_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x33
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d8_f110: .asciz "CIRCLE "


# Test po_t_udg_d9_f110 tests PO_T_UDG when passed A=0xd9 (BASIC keyword "INK")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "INK ".
po_t_udg_d9_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_d9_f110_setup_regs:
  ld      a, 0xd9
  ret

po_t_udg_d9_f110_effects:
  ld      de, msg_po_t_udg_d9_f110
  call    print_msg_de
  ret

po_t_udg_d9_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x34
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_d9_f110: .asciz "INK "


# Test po_t_udg_da_f110 tests PO_T_UDG when passed A=0xda (BASIC keyword "PAPER")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "PAPER ".
po_t_udg_da_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_da_f110_setup_regs:
  ld      a, 0xda
  ret

po_t_udg_da_f110_effects:
  ld      de, msg_po_t_udg_da_f110
  call    print_msg_de
  ret

po_t_udg_da_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x35
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_da_f110: .asciz "PAPER "


# Test po_t_udg_db_f110 tests PO_T_UDG when passed A=0xdb (BASIC keyword "FLASH")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "FLASH ".
po_t_udg_db_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_db_f110_setup_regs:
  ld      a, 0xdb
  ret

po_t_udg_db_f110_effects:
  ld      de, msg_po_t_udg_db_f110
  call    print_msg_de
  ret

po_t_udg_db_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x36
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_db_f110: .asciz "FLASH "


# Test po_t_udg_dc_f110 tests PO_T_UDG when passed A=0xdc (BASIC keyword "BRIGHT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "BRIGHT ".
po_t_udg_dc_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_dc_f110_setup_regs:
  ld      a, 0xdc
  ret

po_t_udg_dc_f110_effects:
  ld      de, msg_po_t_udg_dc_f110
  call    print_msg_de
  ret

po_t_udg_dc_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x37
  ld      e, N_FLAG|X5_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_dc_f110: .asciz "BRIGHT "


# Test po_t_udg_dd_f110 tests PO_T_UDG when passed A=0xdd (BASIC keyword "INVERSE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "INVERSE ".
po_t_udg_dd_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_dd_f110_setup_regs:
  ld      a, 0xdd
  ret

po_t_udg_dd_f110_effects:
  ld      de, msg_po_t_udg_dd_f110
  call    print_msg_de
  ret

po_t_udg_dd_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x38
  ld      e, N_FLAG|X3_FLAG|X5_FLAG       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_dd_f110: .asciz "INVERSE "


# Test po_t_udg_de_f110 tests PO_T_UDG when passed A=0xde (BASIC keyword "OVER")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "OVER ".
po_t_udg_de_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_de_f110_setup_regs:
  ld      a, 0xde
  ret

po_t_udg_de_f110_effects:
  ld      de, msg_po_t_udg_de_f110
  call    print_msg_de
  ret

po_t_udg_de_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x39
  ld      e, N_FLAG|X3_FLAG|X5_FLAG       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_de_f110: .asciz "OVER "


# Test po_t_udg_df_f110 tests PO_T_UDG when passed A=0xdf (BASIC keyword "OUT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "OUT ".
po_t_udg_df_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_df_f110_setup_regs:
  ld      a, 0xdf
  ret

po_t_udg_df_f110_effects:
  ld      de, msg_po_t_udg_df_f110
  call    print_msg_de
  ret

po_t_udg_df_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x3a
  ld      e, N_FLAG|X3_FLAG|X5_FLAG       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_df_f110: .asciz "OUT "


# Test po_t_udg_e0_f110 tests PO_T_UDG when passed A=0xe0 (BASIC keyword "LPRINT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LPRINT ".
po_t_udg_e0_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e0_f110_setup_regs:
  ld      a, 0xe0
  ret

po_t_udg_e0_f110_effects:
  ld      de, msg_po_t_udg_e0_f110
  call    print_msg_de
  ret

po_t_udg_e0_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x3b
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e0_f110: .asciz "LPRINT "


# Test po_t_udg_e1_f110 tests PO_T_UDG when passed A=0xe1 (BASIC keyword "LLIST")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LLIST ".
po_t_udg_e1_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e1_f110_setup_regs:
  ld      a, 0xe1
  ret

po_t_udg_e1_f110_effects:
  ld      de, msg_po_t_udg_e1_f110
  call    print_msg_de
  ret

po_t_udg_e1_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x3c
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e1_f110: .asciz "LLIST "


# Test po_t_udg_e2_f110 tests PO_T_UDG when passed A=0xe2 (BASIC keyword "STOP")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "STOP ".
po_t_udg_e2_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e2_f110_setup_regs:
  ld      a, 0xe2
  ret

po_t_udg_e2_f110_effects:
  ld      de, msg_po_t_udg_e2_f110
  call    print_msg_de
  ret

po_t_udg_e2_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x3d
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e2_f110: .asciz "STOP "


# Test po_t_udg_e3_f110 tests PO_T_UDG when passed A=0xe3 (BASIC keyword "READ")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "READ ".
po_t_udg_e3_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e3_f110_setup_regs:
  ld      a, 0xe3
  ret

po_t_udg_e3_f110_effects:
  ld      de, msg_po_t_udg_e3_f110
  call    print_msg_de
  ret

po_t_udg_e3_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x3e
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e3_f110: .asciz "READ "


# Test po_t_udg_e4_f110 tests PO_T_UDG when passed A=0xe4 (BASIC keyword "DATA")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "DATA ".
po_t_udg_e4_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e4_f110_setup_regs:
  ld      a, 0xe4
  ret

po_t_udg_e4_f110_effects:
  ld      de, msg_po_t_udg_e4_f110
  call    print_msg_de
  ret

po_t_udg_e4_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x3f
  ld      e, N_FLAG|H_FLAG|X3_FLAG|X5_FLAG  
                                          ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e4_f110: .asciz "DATA "


# Test po_t_udg_e5_f110 tests PO_T_UDG when passed A=0xe5 (BASIC keyword "RESTORE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "RESTORE ".
po_t_udg_e5_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e5_f110_setup_regs:
  ld      a, 0xe5
  ret

po_t_udg_e5_f110_effects:
  ld      de, msg_po_t_udg_e5_f110
  call    print_msg_de
  ret

po_t_udg_e5_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x40
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e5_f110: .asciz "RESTORE "


# Test po_t_udg_e6_f110 tests PO_T_UDG when passed A=0xe6 (BASIC keyword "NEW")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "NEW ".
po_t_udg_e6_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e6_f110_setup_regs:
  ld      a, 0xe6
  ret

po_t_udg_e6_f110_effects:
  ld      de, msg_po_t_udg_e6_f110
  call    print_msg_de
  ret

po_t_udg_e6_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x41
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e6_f110: .asciz "NEW "


# Test po_t_udg_e7_f110 tests PO_T_UDG when passed A=0xe7 (BASIC keyword "BORDER")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "BORDER ".
po_t_udg_e7_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e7_f110_setup_regs:
  ld      a, 0xe7
  ret

po_t_udg_e7_f110_effects:
  ld      de, msg_po_t_udg_e7_f110
  call    print_msg_de
  ret

po_t_udg_e7_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x42
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e7_f110: .asciz "BORDER "


# Test po_t_udg_e8_f110 tests PO_T_UDG when passed A=0xe8 (BASIC keyword "CONTINUE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CONTINUE ".
po_t_udg_e8_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e8_f110_setup_regs:
  ld      a, 0xe8
  ret

po_t_udg_e8_f110_effects:
  ld      de, msg_po_t_udg_e8_f110
  call    print_msg_de
  ret

po_t_udg_e8_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x43
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e8_f110: .asciz "CONTINUE "


# Test po_t_udg_e9_f110 tests PO_T_UDG when passed A=0xe9 (BASIC keyword "DIM")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "DIM ".
po_t_udg_e9_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_e9_f110_setup_regs:
  ld      a, 0xe9
  ret

po_t_udg_e9_f110_effects:
  ld      de, msg_po_t_udg_e9_f110
  call    print_msg_de
  ret

po_t_udg_e9_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x44
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_e9_f110: .asciz "DIM "


# Test po_t_udg_ea_f110 tests PO_T_UDG when passed A=0xea (BASIC keyword "REM")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "REM ".
po_t_udg_ea_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ea_f110_setup_regs:
  ld      a, 0xea
  ret

po_t_udg_ea_f110_effects:
  ld      de, msg_po_t_udg_ea_f110
  call    print_msg_de
  ret

po_t_udg_ea_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x45
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ea_f110: .asciz "REM "


# Test po_t_udg_eb_f110 tests PO_T_UDG when passed A=0xeb (BASIC keyword "FOR")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "FOR ".
po_t_udg_eb_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_eb_f110_setup_regs:
  ld      a, 0xeb
  ret

po_t_udg_eb_f110_effects:
  ld      de, msg_po_t_udg_eb_f110
  call    print_msg_de
  ret

po_t_udg_eb_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x46
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_eb_f110: .asciz "FOR "


# Test po_t_udg_ec_f110 tests PO_T_UDG when passed A=0xec (BASIC keyword "GO TO")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "GO TO ".
po_t_udg_ec_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ec_f110_setup_regs:
  ld      a, 0xec
  ret

po_t_udg_ec_f110_effects:
  ld      de, msg_po_t_udg_ec_f110
  call    print_msg_de
  ret

po_t_udg_ec_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x47
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ec_f110: .asciz "GO TO "


# Test po_t_udg_ed_f110 tests PO_T_UDG when passed A=0xed (BASIC keyword "GO SUB")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "GO SUB ".
po_t_udg_ed_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ed_f110_setup_regs:
  ld      a, 0xed
  ret

po_t_udg_ed_f110_effects:
  ld      de, msg_po_t_udg_ed_f110
  call    print_msg_de
  ret

po_t_udg_ed_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x48
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ed_f110: .asciz "GO SUB "


# Test po_t_udg_ee_f110 tests PO_T_UDG when passed A=0xee (BASIC keyword "INPUT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "INPUT ".
po_t_udg_ee_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ee_f110_setup_regs:
  ld      a, 0xee
  ret

po_t_udg_ee_f110_effects:
  ld      de, msg_po_t_udg_ee_f110
  call    print_msg_de
  ret

po_t_udg_ee_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x49
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ee_f110: .asciz "INPUT "


# Test po_t_udg_ef_f110 tests PO_T_UDG when passed A=0xef (BASIC keyword "LOAD")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LOAD ".
po_t_udg_ef_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ef_f110_setup_regs:
  ld      a, 0xef
  ret

po_t_udg_ef_f110_effects:
  ld      de, msg_po_t_udg_ef_f110
  call    print_msg_de
  ret

po_t_udg_ef_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x4a
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ef_f110: .asciz "LOAD "


# Test po_t_udg_f0_f110 tests PO_T_UDG when passed A=0xf0 (BASIC keyword "LIST")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LIST ".
po_t_udg_f0_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f0_f110_setup_regs:
  ld      a, 0xf0
  ret

po_t_udg_f0_f110_effects:
  ld      de, msg_po_t_udg_f0_f110
  call    print_msg_de
  ret

po_t_udg_f0_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x4b
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f0_f110: .asciz "LIST "


# Test po_t_udg_f1_f110 tests PO_T_UDG when passed A=0xf1 (BASIC keyword "LET")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "LET ".
po_t_udg_f1_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f1_f110_setup_regs:
  ld      a, 0xf1
  ret

po_t_udg_f1_f110_effects:
  ld      de, msg_po_t_udg_f1_f110
  call    print_msg_de
  ret

po_t_udg_f1_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x4c
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f1_f110: .asciz "LET "


# Test po_t_udg_f2_f110 tests PO_T_UDG when passed A=0xf2 (BASIC keyword "PAUSE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "PAUSE ".
po_t_udg_f2_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f2_f110_setup_regs:
  ld      a, 0xf2
  ret

po_t_udg_f2_f110_effects:
  ld      de, msg_po_t_udg_f2_f110
  call    print_msg_de
  ret

po_t_udg_f2_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x4d
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f2_f110: .asciz "PAUSE "


# Test po_t_udg_f3_f110 tests PO_T_UDG when passed A=0xf3 (BASIC keyword "NEXT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "NEXT ".
po_t_udg_f3_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f3_f110_setup_regs:
  ld      a, 0xf3
  ret

po_t_udg_f3_f110_effects:
  ld      de, msg_po_t_udg_f3_f110
  call    print_msg_de
  ret

po_t_udg_f3_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x4e
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f3_f110: .asciz "NEXT "


# Test po_t_udg_f4_f110 tests PO_T_UDG when passed A=0xf4 (BASIC keyword "POKE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "POKE ".
po_t_udg_f4_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f4_f110_setup_regs:
  ld      a, 0xf4
  ret

po_t_udg_f4_f110_effects:
  ld      de, msg_po_t_udg_f4_f110
  call    print_msg_de
  ret

po_t_udg_f4_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x4f
  ld      e, N_FLAG|H_FLAG|X3_FLAG        ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f4_f110: .asciz "POKE "


# Test po_t_udg_f5_f110 tests PO_T_UDG when passed A=0xf5 (BASIC keyword "PRINT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "PRINT ".
po_t_udg_f5_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f5_f110_setup_regs:
  ld      a, 0xf5
  ret

po_t_udg_f5_f110_effects:
  ld      de, msg_po_t_udg_f5_f110
  call    print_msg_de
  ret

po_t_udg_f5_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x50
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f5_f110: .asciz "PRINT "


# Test po_t_udg_f6_f110 tests PO_T_UDG when passed A=0xf6 (BASIC keyword "PLOT")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "PLOT ".
po_t_udg_f6_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f6_f110_setup_regs:
  ld      a, 0xf6
  ret

po_t_udg_f6_f110_effects:
  ld      de, msg_po_t_udg_f6_f110
  call    print_msg_de
  ret

po_t_udg_f6_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x51
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f6_f110: .asciz "PLOT "


# Test po_t_udg_f7_f110 tests PO_T_UDG when passed A=0xf7 (BASIC keyword "RUN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "RUN ".
po_t_udg_f7_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f7_f110_setup_regs:
  ld      a, 0xf7
  ret

po_t_udg_f7_f110_effects:
  ld      de, msg_po_t_udg_f7_f110
  call    print_msg_de
  ret

po_t_udg_f7_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x52
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f7_f110: .asciz "RUN "


# Test po_t_udg_f8_f110 tests PO_T_UDG when passed A=0xf8 (BASIC keyword "SAVE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "SAVE ".
po_t_udg_f8_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f8_f110_setup_regs:
  ld      a, 0xf8
  ret

po_t_udg_f8_f110_effects:
  ld      de, msg_po_t_udg_f8_f110
  call    print_msg_de
  ret

po_t_udg_f8_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x53
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f8_f110: .asciz "SAVE "


# Test po_t_udg_f9_f110 tests PO_T_UDG when passed A=0xf9 (BASIC keyword "RANDOMIZE")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "RANDOMIZE ".
po_t_udg_f9_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_f9_f110_setup_regs:
  ld      a, 0xf9
  ret

po_t_udg_f9_f110_effects:
  ld      de, msg_po_t_udg_f9_f110
  call    print_msg_de
  ret

po_t_udg_f9_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x54
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_f9_f110: .asciz "RANDOMIZE "


# Test po_t_udg_fa_f110 tests PO_T_UDG when passed A=0xfa (BASIC keyword "IF")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "IF ".
po_t_udg_fa_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_fa_f110_setup_regs:
  ld      a, 0xfa
  ret

po_t_udg_fa_f110_effects:
  ld      de, msg_po_t_udg_fa_f110
  call    print_msg_de
  ret

po_t_udg_fa_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x55
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_fa_f110: .asciz "IF "


# Test po_t_udg_fb_f110 tests PO_T_UDG when passed A=0xfb (BASIC keyword "CLS")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CLS ".
po_t_udg_fb_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_fb_f110_setup_regs:
  ld      a, 0xfb
  ret

po_t_udg_fb_f110_effects:
  ld      de, msg_po_t_udg_fb_f110
  call    print_msg_de
  ret

po_t_udg_fb_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x56
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_fb_f110: .asciz "CLS "


# Test po_t_udg_fc_f110 tests PO_T_UDG when passed A=0xfc (BASIC keyword "DRAW")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "DRAW ".
po_t_udg_fc_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_fc_f110_setup_regs:
  ld      a, 0xfc
  ret

po_t_udg_fc_f110_effects:
  ld      de, msg_po_t_udg_fc_f110
  call    print_msg_de
  ret

po_t_udg_fc_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x57
  ld      e, N_FLAG                       ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_fc_f110: .asciz "DRAW "


# Test po_t_udg_fd_f110 tests PO_T_UDG when passed A=0xfd (BASIC keyword "CLEAR")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "CLEAR ".
po_t_udg_fd_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_fd_f110_setup_regs:
  ld      a, 0xfd
  ret

po_t_udg_fd_f110_effects:
  ld      de, msg_po_t_udg_fd_f110
  call    print_msg_de
  ret

po_t_udg_fd_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x58
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_fd_f110: .asciz "CLEAR "


# Test po_t_udg_fe_f110 tests PO_T_UDG when passed A=0xfe (BASIC keyword "RETURN")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "RETURN ".
po_t_udg_fe_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_fe_f110_setup_regs:
  ld      a, 0xfe
  ret

po_t_udg_fe_f110_effects:
  ld      de, msg_po_t_udg_fe_f110
  call    print_msg_de
  ret

po_t_udg_fe_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x59
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_fe_f110: .asciz "RETURN "


# Test po_t_udg_ff_f110 tests PO_T_UDG when passed A=0xff (BASIC keyword "COPY")
# with bit 0 of [IY+1] set to 1 (leading space suppressed) when used with
# a mock print-out routine that doesn't disturb any registers.
# Expected output is "COPY ".
po_t_udg_ff_f110_setup:
  _strh   fake_channel_block, CURCHL      ; test channel block that uses fake_printout routine
  _setbit 0, FLAGS                        ; leading space suppressed
  _setbit 1, FLAGS                        ; printer in use (used by po_fetch)
  _strb   0x46, P_POSN_X                  ; current printer cursor position
  _strh   0x3527, PR_CC                   ; current (fake) printer buffer address
  _resbit 0, TV_FLAG                      ; lower screen not in use (used by po_fetch)
  _strh   0x352f, S_POSN_X                ; current upper screen cursor position
  _strh   0x1bce, DF_CC                   ; current (fake) upper screen address in display file
  _strh   0x4002, 0x4000                  ; set start address of output
  ret

po_t_udg_ff_f110_setup_regs:
  ld      a, 0xff
  ret

po_t_udg_ff_f110_effects:
  ld      de, msg_po_t_udg_ff_f110
  call    print_msg_de
  ret

po_t_udg_ff_f110_effects_regs:
  ld      a, ' '
  ldf     H_FLAG|X3_FLAG
  ld      d, 0x5a
  ld      e, N_FLAG|X3_FLAG               ; entry A SUB 165 (0xa5)
  ld      c, 0x46
  ld      hl, 0x3527
  ret

msg_po_t_udg_ff_f110: .asciz "COPY "
